{
  "buildCommand": "npm run build:client",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    }
  ],
  "env": {
    "NODE_ENV": "production",
    "JWT_SECRET": "quantumfit-jwt-secret-key",
    "SESSION_SECRET": "quantumfit-super-secret-key"
  }
}
